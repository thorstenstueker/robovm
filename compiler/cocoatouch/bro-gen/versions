# What the bindings are generated with. generate.sh refuses to run against anything else, so that
# two machines produce the same Java from the same headers. Bump deliberately, one line at a time.
XCODE=27.0
IOS_SDK=27.0
BRO_GEN_REPO=https://github.com/thorstenstueker/robovm-bro-gen.git
BRO_GEN_COMMIT=e495077
RUBY_MIN=3.2
FFI_MIN=1.17
FFI_CLANG_MIN=0.10
